require("noice").setup({
    lsp = {
        hover = {
            enabled = false,
        }
        -- message = {
        --  enabled = true,
        --  view = "notify",
        --  opts = {},
        -- }
    },
})
